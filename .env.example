# Copier ce fichier en .env et remplir les valeurs

# PostgreSQL Neon (déjà configuré dans server.ts, pas besoin de changer)
DATABASE_URL=postgresql://neondb_owner:...@.../neondb?sslmode=require

# Clé API Gemini (optionnel)
GEMINI_API_KEY=
